#!/bin/bash
cd ${0%/*} || exit 1                        # Run from this directory

rm -rf log* processor* postProcessing* *.out
rm -rf *[1-9]*

#------------------------------------------------------------------------------
